export const isLoading = state => state.isLoading;

export const isReady = state => state.isReady;

export const snackbar = state => state.snackbar;

export const isPlaying = state => state.isPlaying;

export const currentSongId = state => state.songId;
