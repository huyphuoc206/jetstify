server:
  port: 8081

spring:
  datasource:
    password: 123456
    url: jdbc:postgresql://localhost:5432/example
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
