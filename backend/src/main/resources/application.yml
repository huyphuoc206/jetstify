server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/example
    username: postgres
    password: 1
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

jestify-app:
  token:
    access_lifetime_minutes: 1
    refresh_lifetime_minutes: 1440
  remember_me:
    expired_days: 30